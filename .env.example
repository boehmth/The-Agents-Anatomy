# Copy to .env and fill in.
# Which LLM provider to use.
#   gemini = Google Gemini (needs GOOGLE_API_KEY)
#   openai = OpenAI (needs OPENAI_API_KEY)
#   deepseek = DeepSeek (needs DEEPSEEK_API_KEY)
#   sap    = SAP GenAI Hub on BTP (needs a service key JSON, see below)
LLM_PROVIDER=gemini

# --- Provider: gemini ---
GOOGLE_API_KEY=

# Optional: override the default model.
# GEMINI_MODEL=models/gemini-flash-latest

# --- Provider: openai ---
OPENAI_API_KEY=

# Optional: override the default model.
# OPENAI_MODEL=gpt-4o-mini

# Optional: temperature (Default 1). Manche Modelle (z. B. gpt-5.x)
# unterstützen nur den Wert 1.
# OPENAI_TEMPERATURE=1

# Optional: max. Ausgabe-Tokens.
# OPENAI_MAX_OUTPUT_TOKENS=4096

# --- Provider: deepseek ---
# DeepSeek nutzt eine OpenAI-kompatible API.
DEEPSEEK_API_KEY=

# Optional: override the default model.
# DEEPSEEK_MODEL=deepseek-chat

# Optional: Base-URL (Default: https://api.deepseek.com).
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# Optional: temperature (Default 1).
# DEEPSEEK_TEMPERATURE=1

# Optional: max. Ausgabe-Tokens.
# DEEPSEEK_MAX_OUTPUT_TOKENS=4096

# --- Provider: sap ---
# Path to the BTP service key JSON file (keep this file OUT of git!).
SAP_GENAI_SERVICE_KEY_FILE=./.sap_service_key.json

# Model to use. Must be a running deployment in your AI Core resource group.
# Common values: gemini-1.5-pro, gemini-1.5-flash, gpt-4o-mini, gpt-4o,
#                anthropic--claude-4.5-haiku, anthropic--claude-4.7-opus
SAP_GENAI_MODEL=gemini-1.5-flash

# Resource group inside AI Core (usually 'default').
SAP_GENAI_RESOURCE_GROUP=default

# --- Prompts ---
# Which prompt version the runner should use.
# Reads from prompts/<PROMPT_VERSION>/system_prompt.txt and tool_descriptions.txt
PROMPT_VERSION=v1

# --- Fake Store API ---
# Optional: override the Fake Store API endpoint.
# FAKE_STORE_URL=https://fakestoreapi.com/products
